'use client';

// This layout file is used to override the root layout for the dashboard section
// It removes the global navbar and footer for admin pages

export default function DashboardLayout({ children }) {
  return children;
}
